import { LoadingIndicator } from '@/components/ui/LoadingIndicator';

export default function RootLoading() {
  return <LoadingIndicator label="Abrindo o SocialHub" description="Só um instante, estamos preparando seu espaço." />;
}
